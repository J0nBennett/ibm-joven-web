---
import Layout from '../layouts/Layout.astro';

const albunes = [
    { titulo: "Campamento 2025", url: "#", count: "120 fotos" },
    { titulo: "Día de la Primavera", url: "#", count: "45 fotos" },
    { titulo: "Aniversario Joven", url: "#", count: "80 fotos" }
];
---

<Layout title="Galería - IBM Joven">
    <section class="max-w-4xl mx-auto text-center">
        <h1 class="text-3xl font-bold text-gray-900 mb-4">Galería de Fotos</h1>
        <p class="text-gray-600 mb-12">Revive los mejores momentos de nuestras actividades.</p>

        <div class="grid md:grid-cols-3 gap-6">
            {albunes.map(album => (
                <a href={album.url} target="_blank" class="block group">
                    <div class="bg-gray-200 aspect-video rounded-lg mb-3 flex items-center justify-center group-hover:bg-gray-300 transition relative overflow-hidden">
                         <span class="text-4xl text-gray-400">📷</span>
                         <div class="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-10 transition"></div>
                    </div>
                    <h3 class="font-bold text-lg text-gray-800 group-hover:text-blue-700 transition">{album.titulo}</h3>
                    <p class="text-sm text-gray-500">{album.count}</p>
                </a>
            ))}
        </div>
        
        <div class="mt-16 p-8 bg-blue-50 rounded-lg">
            <h3 class="font-bold text-blue-900 mb-2">¿Tienes fotos para compartir?</h3>
            <p class="text-blue-800">Envíanos tus fotos al equipo de multimedia para agregarlas a los álbumes oficiales.</p>
        </div>
    </section>
</Layout>
